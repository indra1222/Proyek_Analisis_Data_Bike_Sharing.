Fitur Dashboard
Analisis data e-commerce publik
Visualisasi distribusi berat produk berdasarkan kategori
Analisis korelasi panjang deskripsi dan jumlah foto produk terhadap berat dan ukuran produk
Pembersihan data dan deteksi outlier
Setup Environment
1. Menggunakan Anaconda (Rekomendasi)
Buat lingkungan baru dengan Python 3.9:
bash
Copy code
conda create --name main-ds python=3.9
Aktifkan lingkungan tersebut:
bash
Copy code
conda activate main-ds
Install dependencies yang diperlukan:
bash
Copy code
pip install -r requirements.txt
2. Menggunakan Shell/Terminal
Buat folder proyek:
bash
Copy code
mkdir proyek_analisis_data
cd proyek_analisis_data
Inisialisasi virtual environment dengan Pipenv:
bash
Copy code
pipenv install
Masuk ke virtual environment:
bash
Copy code
pipenv shell
Install dependencies:
bash
Copy code
pip install -r requirements.txt
Menjalankan Aplikasi Streamlit
Setelah setup environment selesai, Anda bisa menjalankan aplikasi Streamlit dengan menjalankan perintah berikut di terminal:

bash
Copy code
streamlit run dashboard.py
Dashboard akan berjalan di localhost dan dapat diakses melalui web browser di URL yang ditampilkan, misalnya http://localhost:8501.
